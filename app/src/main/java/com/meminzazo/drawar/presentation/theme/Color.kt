package com.meminzazo.drawar.presentation.theme

import androidx.compose.ui.graphics.Color


// Fondos
val BackgroundDark = Color(0xFF0D0D0D)
val SurfaceDark = Color(0xFF1A1A1A)
val SurfaceVariant = Color(0xFF2A2A2A)

// Acento principal — ámbar suave, visible sin ser agresivo
val Amber400 = Color(0xFFFFCA28)
val Amber200 = Color(0xFFFFE082)

// Estados
val OnSurface = Color(0xFFE8E8E8)
val OnSurfaceMuted = Color(0xFF888888)
val ErrorRed = Color(0xFFCF6679)

// Overlay UI — semitransparente para no tapar la cámara
val ControlBackground = Color(0xCC1A1A1A)  // 80% opaco
